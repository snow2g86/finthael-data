---
type: inbox_draft
trigger: streak_loss
symbol: ETHUSDT
side: short
position_id: 7193
realized_pnl: -0.2349
closed_kst: 2026-05-27 10:20
strategy: contra_volgate_demo
generated_by: claude-claude-sonnet-4-6
generated_at: 2026-05-27T01:30:47+09:00
---

# streak_loss — ETHUSDT short pos#7193

## 관찰

오늘 KST 09:27 ~ 10:20, 약 53분 사이에 ETHUSDT에서 세 차례 연속 손실이 발생했다.

| # | ID | 방향 | 종료 사유 | PNL ($) | 소요 시간 |
|---|-----|------|-----------|---------|-----------|
| 1 | 7171 | long | timeout | −1.4956 | 장시간 (타임아웃) |
| 2 | 7187 | long | fast_sl | −0.3388 | 짧음 |
| 3 | 7193 | short | manual_stop | −0.2349 | 약 4분 |

눈에 띄는 수치: 마지막 거래(7193)의 `max_pnl_pct`가 **+8.93%** 였음에도 −$0.23으로 마감되었다. 즉, 수익 구간이 실제로 열렸다가 반전된 것으로 보인다. 또한 `original_sl = 0.0`, `atr_pct = null`로 기록되어 있어 손절·변동성 파라미터가 이 거래에서 정상 작동했는지 의심스럽다.

---

## 해석 가설

1. **방향 전환의 역설(추정)**: 전략 명칭 `contra_volgate`는 변동성 게이트 신호의 역방향 진입이 핵심이다. long 두 번 실패 후 short로 전환한 것은, 시장이 "진짜 추세 구간"에 진입해 mean-revert 가정 자체가 무효화된 상황일 가능성이 있다. 53분 안에 3회 진입을 반복한 것은 추세 장에서 전략이 계속 역방향 베팅을 시도한 패턴과 유사하다.

2. **이익 실현 로직 부재 가설**: +8.93% peak가 존재했음에도 `manual_stop`으로 −$0.23에 종료된 점은, trailing stop 또는 partial TP가 동작하지 않았음을 시사한다. 수익 구간에서 자동 청산이 이뤄졌다면 결과가 달랐을 추정이다.

3. **SL 미설정 위험(추정)**: `original_sl = 0.0`이 의도적 설정인지 버그인지 불분명하나, SL 없이 레버리지 5배 진입은 manual_stop 의존도를 높이는 구조적 취약점으로 보인다.

---

## 검증 아이디어

- **추세 필터 백테스트**: 진입 시점 15분봉 EMA(20/50) 크로스 또는 ADX ≥ 25 조건에서 contra 진입을 제외하면 연속 손실 빈도가 줄어드는지 확인.
- **streak 쿨다운 효과 측정**: 같은 종목 2연속 손실 시 1시간 진입 금지를 적용했을 때 전체 샤프 비율 변화량 추정.
- **TP trailing 로직 유무 확인**: `max_pnl_pct > 5%` 이후 반전 종료된 거래 비율을 집계해 이익 실현 로직 필요성을 수치로 평가.
- **`original_sl = 0` 발생 조건 재현**: 어떤 진입 경로에서 SL이 0으로 기록되는지 코드 레벨 추적 필요.

---

## 잠정 액션

1. **추세 필터 도입 검토**: mean-revert 전략이므로, 강추세 구간 진입 자제 로직(ADX 또는 볼린저 밴드 확장 감지)을 후보로 올린다.
2. **streak 쿨다운 파라미터 설정**: `streak_loss` 트리거 발생 시 해당 종목 재진입에 최소 30~60분 대기 적용 여부를 결정 대기.
3. **SL 기본값 강제화**: `original_sl = 0.0`이 발생하지 않도록 ATR 기반 SL 계산 실패 시 진입 자체를 차단하는 방어 로직 추가 가설.
4. **Trailing TP 파일럿**: peak PNL 도달 후 일정 비율 하락 시 자동 청산하는 trailing stop 파라미터를 demo 환경에서 A/B로 테스트.

> 이 노트는 AI draft 입니다 — 사용자 검토 후 분류/보강 필요.
