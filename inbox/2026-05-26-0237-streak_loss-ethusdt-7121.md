---
type: inbox_draft
trigger: streak_loss
symbol: ETHUSDT
side: short
position_id: 7121
realized_pnl: -0.0673
closed_kst: 2026-05-26 02:37
strategy: contra_volgate_demo
generated_by: claude-claude-sonnet-4-6
generated_at: 2026-05-25T17:40:37+09:00
---

# streak_loss — ETHUSDT short pos#7121

## 관찰

| 항목 | 값 |
|---|---|
| 대상 | ETHUSDT (Bybit demo perp) |
| 기간 | 01:01 → 02:37 KST (약 96분) |
| 거래 수 | 3회 연속 |
| 방향 순서 | short → long → short |
| 청산 사유 | 3회 모두 `fast_sl` |
| 누적 PnL | −$0.1326 − $0.2550 − $0.0673 = **−$0.455** |
| 최대 수익 구간 | id 7121 기준 `max_pnl_pct` +0.024% — 진입 직후 단기 유리한 구간 존재 |
| ATR 데이터 | `atr_pct: null` — 변동성 지표 누락 |

3회 모두 `fast_sl`로 조기 청산된 점과, short → long → short로 방향이 전환된 점이 주목된다. `original_sl: 0.0`은 가격 기반 SL이 아닌 금액/틱 기준으로 설정되었거나, 설정이 누락되었을 가능성을 시사한다.

---

## 해석 가설

1. **추세 지속 구간 진입 가설**: 01:00–02:30 KST (16:00–17:30 UTC)는 미국 장 초반 오버랩 구간으로, ETH가 단방향 추세를 형성했을 가능성이 있다. 평균회귀(mean-revert) 전략은 추세 지속 구간에서 구조적으로 불리하다는 추정이다.

2. **휩소(whipsaw) 반복 가설**: short 실패 후 반대 방향(long)으로 재진입한 것은 전략이 노이즈를 "새로운 변동성 신호"로 오인했을 가능성이 있다. 같은 종목에서 빠르게 방향을 뒤집는 패턴은 변동성 게이트 기준값이 너무 낮게 설정되어 있을 때 발생한다는 추정이다.

3. **fast_sl 임계값 문제 가설**: 3회 모두 `fast_sl`로 청산되었다는 것은 가격이 "짧고 강한 역방향 스파이크" 후 복귀하는 패턴일 가능성이 있다. SL 거리가 시장 노이즈 수준보다 좁을 경우 정상적인 변동에도 청산된다는 추정이다.

---

## 검증 아이디어

- **시간대 필터**: 동일 전략을 UTC 16:00–18:00(미국 장 초반) 구간에서 backreview하여 해당 시간대 win rate가 다른 구간 대비 유의미하게 낮은지 확인
- **연속 손실 쿨다운**: 같은 종목에서 N회(예: 2회) 연속 fast_sl 발생 시 일정 시간(30–60분) 해당 종목 진입 차단 규칙 효과 시뮬레이션
- **ATR 기반 SL 재검토**: `atr_pct: null` 상태에서 fast_sl이 작동한 경위 확인 — ATR이 없는 경우 고정 틱 SL로 fallback하는지 코드 레벨 검증

---

## 잠정 액션

- **즉시(가설 수준)**: streak_loss 트리거 발동 시 해당 종목을 자동으로 30분 블랙리스트에 추가하는 로직 검토
- **중기(검증 후)**: 변동성 게이트 진입 조건에 "추세 강도 필터"(예: ADX > 25일 경우 역방향 진입 스킵) 추가 여부 논의
- **데이터 보완**: `atr_pct: null` 발생 원인 규명 — ATR 계산 시점과 진입 시점 간 race condition 추정

> 이 노트는 AI draft 입니다 — 사용자 검토 후 분류/보강 필요.
