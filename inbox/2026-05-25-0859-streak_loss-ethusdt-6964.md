---
type: inbox_draft
trigger: streak_loss
symbol: ETHUSDT
side: long
position_id: 6964
realized_pnl: -0.0629
closed_kst: 2026-05-25 08:59
strategy: contra_volgate_demo
generated_by: claude-claude-sonnet-4-6
generated_at: 2026-05-25T00:10:42+09:00
---

# streak_loss — ETHUSDT long pos#6964

## 관찰

같은 종목(ETHUSDT)에서 72시간 이내 3회 연속 손실이 발생했으며, 세 건 모두 `fast_sl`로 청산됐다.

| 구분 | ID | 방향 | 청산 원인 | 실현 PnL | 특이사항 |
|------|-----|------|-----------|----------|----------|
| 1차 | 6940 | short | fast_sl | **-$0.5725** | 손실 규모 이상 |
| 2차 | 6954 | long | fast_sl | -$0.0451 | 08:34 KST |
| 3차 (현재) | 6964 | long | fast_sl | -$0.0629 | 08:59 KST, 2차 후 **25분** 재진입 |

주목할 이상 신호:
- `original_sl = 0.0` — SL 값이 0으로 기록됨 (미설정 or 직렬화 오류 추정)
- `atr_pct = null` — ATR 계산 누락, 포지션 사이즈·SL 거리 산출 근거 불분명
- `max_pnl_pct ≈ 0.07%` — 진입 후 최대 수익이 극히 미미하여 순방향 모멘텀 자체가 없었을 가능성

---

## 해석 가설

**가설 A — 추세 구간 오진입**: 변동성 게이트가 "고변동 종목"을 선별했더라도, ETH가 해당 시점에 mean-revert보다 **단방향 추세**를 타고 있었다면 역방향 진입은 구조적으로 불리하다. max_pnl_pct가 극히 작다는 점이 이를 지지하는 간접 증거로 추정된다.

**가설 B — 재진입 쿨다운 부재**: 2차(08:34)와 3차(08:59) 사이 간격이 25분에 불과하다. 동일 종목 직전 손실을 반영하지 않고 즉시 같은 방향으로 재진입한 것은 전략 로직에 **종목별 쿨다운** 조건이 없음을 시사한다.

**가설 C — SL/ATR 계산 결함**: `original_sl = 0.0`, `atr_pct = null`의 조합이 단순 로깅 문제인지, 실제로 SL이 제대로 설정되지 않아 `fast_sl` 트리거 거리가 비정상적으로 좁아진 것인지 불분명하다. 전자라면 무해하지만 후자라면 손실 확대 경로가 된다.

---

## 검증 아이디어

1. **시장 국면 검증**: 3회 손실 발생 시간대의 ETHUSDT 1시간봉 ADX·EMA 기울기 확인 — 추세 강도가 역방향 진입 조건과 충돌하는지 측정
2. **SL 로그 검증**: `original_sl = 0.0`이 기록된 거래 전수 조회 → DB Null vs 실제 0 구분 + 해당 거래의 실제 SL 주문 체결 이력 대조
3. **ATR null 원인 추적**: `atr_pct`가 null인 거래 비율 집계 및 해당 시점 ATR 계산 루틴 로그 점검
4. **종목별 연패 패턴**: ETHUSDT 외 타 종목의 `fast_sl` 연속 발생률 비교 → ETH에 구조적 편향이 있는지 판단

---

## 잠정 액션

- **[검토 후 결정]** 동일 종목 2연속 손실 시 해당 세션 진입 차단하는 종목별 쿨다운 파라미터 추가 검토
- **[즉시 확인]** `original_sl = 0.0` / `atr_pct = null` 케이스의 재현 조건 파악 — 로깅 버그라면 수정, 로직 결함이라면 SL 설정 로직 패치 우선
- **[백테스트]** contra_volgate 전략의 종목별 승률 분포 확인, ETH가 하위 성과 종목이면 변동성 게이트 필터 기준 조정 고려

> 이 노트는 AI draft 입니다 — 사용자 검토 후 분류/보강 필요.
